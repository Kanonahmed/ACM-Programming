#include<bits/stdc++.h>
using namespace std;
int main()
{
    //vector<int>v; ////one dimention
    vector<int>vv[10]; //// two dimention here 10 row
    vector< vector<int> >vp;  // two dimention
    queue<int>q[10];
    //q.push(something);
    priority_queue<int> qq[10];
    vector<int>:: iterator i;
    set<int> s[10];
    //s.insert(something);
    pair<int ,int> p[10];
    //p.first// p.second
    vector< int > v;
    for(int i=0; i<11; i++) v.push_back( i );

    do {
    // protitat jama prottekke porai dekho shukh maximize hochche kina
    }while( next_permutation( v.begin(), v.end() ) );





return 0;
}
